#! /usr/bin/env bash

# vbox exec -[i|null] :vname [arg...]

# check vname availability

if [[ -z "$vname" ]]
then
	echo "Missing virtual machine name"
	exit 1
fi

# resolve vname
resolve_vname $vname

declare -i rcode=0

connect_vm_host

rcode=$?
